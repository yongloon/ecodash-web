/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Optional: Add other Next.js configurations here if needed
    // Example: environment variables accessible on the client-side
    // env: {
    //   NEXT_PUBLIC_EXAMPLE_VAR: process.env.EXAMPLE_VAR,
    // },
  };
  
  export default nextConfig; // Use export default for .mjs files
  // module.exports = nextConfig; // Use module.exports for .js files
  